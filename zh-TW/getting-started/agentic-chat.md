---
title: "Kimi 入門"
slug: "agentic-chat"
order: 2
extract_headings: false
preview: false
---

<SeoMeta
  title="Kimi 入門 - Kimi 說明中心"
  description="你可以在聊天框輸入問題、上傳檔案，或依照需求切換不同模型。"
/>

# Kimi 入門

你可以在聊天框輸入問題、上傳檔案，或依照需求切換不同模型。
開啟 [kimi.com](https://www.kimi.com)，試著傳送一則訊息，和 Kimi 打聲招呼：

<CodePreview
  files={[
    {
      name: "prompt.txt",
      language: "text",
      content: "嗨，Kimi！",
    },
  ]}
/>

## 由 K2.6 驅動
Kimi K2.6 是在 K2.5 基礎上大幅升級的開源模型，聚焦於頂尖的程式撰寫、長週期執行與 Agent Swarm 能力。它在 agent 任務、程式撰寫、圖像理解、影片理解與通用智慧基準測試中，皆達到業界頂尖表現。K2.6 也是 Kimi **最全能**的模型——其原生多模態架構同時支援視覺與文字輸入、Thinking mode 與非 Thinking mode，以及對話與代理式任務。
因此，Kimi 不只擅長問答對話，也能自主規劃任務、呼叫工具並進行多步推理，為複雜任務提供深入思考與執行能力。
### 內建工具
開箱即用，無需設定：
| 工具 | 說明 |
|------|------------|
| **Web Search** | 即時網路搜尋最新新聞與資訊 |
| **擷取工具** | 直接存取特定 URL（網頁、文件、圖片與其他線上資源） |
| **資料來源** | 連接專業資料庫；查詢資料庫清單並擷取資料 |
| **圖片搜尋** | 以文字搜圖、以圖搜圖，快速探索視覺內容 |
| **IPython** | 執行 Python 程式碼，用於資料分析與視覺化 |
| **記憶** | 智慧記憶，記錄你的個人偏好與使用習慣 |

## 視覺與多模態能力
K2.6 支援對圖片、影片與辦公文件（PDF/Word/Excel）的全場景理解，採用**原生多模態架構**（不是外掛式 OCR）。它可以直接解析文件內容，並執行中高階辦公任務（例如撰寫 Excel 公式，或生成 PPT 大綱／VBA 程式碼）。
### 多模態輸入支援
- **圖片**：PNG、JPEG、WebP、GIF 等（透過 URL、Base64 或本機檔案上傳）
- **影片**：MP4 等（透過 video_url，或圖片影格序列 + fps 參數）
- **文件**：PDF、Word、Excel、TXT、Markdown 等
- **線上資源**：URL、文章連結
- **圖片搜尋**：上傳圖片以搜尋相關或相似圖片
- **影片理解**：支援影片理解——可分析螢幕錄影並複現互動邏輯
- **視覺程式撰寫**：上傳螢幕截圖或螢幕錄影以生成前端程式碼（HTML/Vue/React 等）；可依設計稿自動還原網頁

### 輸出格式
- 純文字／圖文混合：標準對話式回覆
- Markdown：包含標題、清單、表格與連結的富文字
- 程式碼區塊：HTML/Vue/React/Python/JSON/Mermaid 等（含語言識別碼）
- JSON 結構化資料：強制以 JSON mode 輸出
- Mermaid 圖表：流程圖、時序圖、心智圖等
- LaTeX 數學公式：行內與區塊層級的數學表達式
- 雙模式輸出（Thinking）：同時回傳 `reasoning_content`（思考過程）與 `content`（最終答案）
- 工具呼叫格式：Function Calling 標準格式（供開發者使用）

### 注意事項

<Callout type="warning">
1. **圖片生成限制**：標準聊天模式僅支援圖片*搜尋*，不支援圖片*生成*（圖片生成需要使用 K2.6 Agent mode）。
2. **檔案輸出限制**：僅支援輸出文字內容（例如 PPT 大綱、Word 內文、程式碼）。不支援直接輸出二進位檔案（`.pptx`、`.docx`、`.xlsx`、`.pdf`）。
3. 若要生成圖片或輸出複雜且可編輯的文件，請使用 [K2.6 Agent](https://www.kimi.com/agent) 或程式碼執行工具。
</Callout>

## Session 與對話回合
當你點擊「新聊天」時，可以把它想成是和 Kimi 開始一段長對話。
| 概念 | 類比 | 技術定義 |
|---------|---------|---------------------|
| **Session** | 一整場會議（從開始到結束） | 一個有明確起點與終點的完整聊天流程 |
| **回合** | 一次往返（你發言，Kimi 回覆） | 你的輸入與 Kimi 的回應構成一次來回 |

### 主要差異
**Session**
- **連續記憶**：Kimi 會記住目前聊天中的所有上下文
- **跨回合上下文**：每次回覆都會參照先前的對話內容，作為下一回合的上下文
- **邊界明確**：點擊「新聊天」= 重新開始；先前上下文會被清除
**回合**
- **累計計數**：第 1 回合、第 2 回合……依序增加
- **記憶有限**：回合太多（例如 50+）可能會讓 Kimi「忘記」較早的內容，尤其是在長文寫作或有嚴格要求的任務中
- **上下文長度限制**：受 token 數限制；當回合累積時，最早的內容可能會被壓縮

<Callout type="tip">
**邊界情況**：如果一次問答就結束了對話，那麼 1 回合 = 1 個 session。
</Callout>

## 為什麼這項區分很重要
在同一個 session 中，你送出的每個新問題或指令，都會參照先前所有回合來回答。隨著回合累積、內容變長，回覆品質可能會逐漸下降。

### 常見情境
**情境 1：切換主題**
- 錯誤做法：在一個已經用來撰寫 30,000 字小說的 session 裡，詢問「幫我計算稅金」
- 正確做法：為稅務問題開啟新的 session，避免 Kimi 把小說角色和稅務概念混在一起
**情境 2：處理長文件**
- 經過數十個回合後，Kimi 可能會「忘記」一開始建立的規則或檔案摘要
- 解法：在任務進行中主動摘要（「總結一下我們目前的計畫」），或開啟新的 session
**情境 3：理解產品限制**
- 上下文長度限制是以 token 衡量，但日常使用時，可以把它理解為「回合數」
- 如果你看到「開始新的聊天」提示，表示目前 session 的回合數已經太多
## 最佳實務

<Callout type="tip">
1. **每個任務使用新的 session**：彼此獨立的任務——寫作、研究、除錯——都應各自開一個 session，避免上下文互相干擾。
2. **長 session 定期摘要**：超過 20 回合後，主動請 Kimi 摘要關鍵資訊，避免資料遺失。
3. **及時清理敏感資訊**：聊天記錄會被保留；處理私人內容後，請刪除整個 session。
</Callout>
